<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hj">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hj">
        <activity android:name=".pedometerelistactivity"></activity>
        <activity android:name=".snoozestop" />
        <activity android:name=".sleep1" />
        <activity android:name=".yesno" />
        <activity android:name=".watertime" />
        <activity android:name=".water1" />
        <activity android:name=".fetchdata" />
        <activity android:name=".exercise1" />
        <activity android:name=".recordpedometer" />
        <activity android:name=".pedometer1" />
        <activity android:name=".login" />
        <activity android:name=".dashboard" />
        <activity android:name=".signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <receiver android:name=".ReminderBroadcast" />
        <receiver android:name=".ReminderBroadcastsleep" />

        <service android:name=".StepsService" />
    </application>

</manifest>